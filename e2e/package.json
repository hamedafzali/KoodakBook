{
  "name": "koodakbook-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests (Playwright) for KoodakBook",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
