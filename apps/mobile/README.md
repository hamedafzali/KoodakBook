# KoodakBook Mobile

React Native app (Expo) — planned for V2 after web MVP ships.

Shares types from `@koodakbook/shared` and talks to the same `@koodakbook/backend`.

## Setup (when ready)

```bash
npx expo install
npx expo start
```
